{
  "name": "isonim-tui-serve-e2e",
  "private": true,
  "version": "0.0.1",
  "description": "Playwright end-to-end tests for isonim-tui-serve. Drives a real Chromium against xterm.js while a real isonim-tui-serve subprocess hosts the echo_packet_app fixture.",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
